- Server is a mess. it needs to be cleaned up. Removing what is not used inside the Server folder. 
- Sorting up the issues for the social events not re-triggering automatically each week. 
- making my localhost .env file properly. 
- making the shop schedule and shop admin accessible via an admin login on the server. I do not want to ssh into the server to change schedules, if I can access an admin-dashboard with a login prompt to have all the useful stuff an admin can have (see players, leaderboards, social events plannings, etc...)

